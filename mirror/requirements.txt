# ============================================================
# These packages are installed via apt on Raspberry Pi OS.
# Run this first:
#   sudo apt install python3-opencv python3-pyaudio python3-pyqt5 python3-numpy python3-pil portaudio19-dev -y
# Then recreate your venv with:
#   python3 -m venv --system-site-packages ~/mirror-venv
# ============================================================

# Pure Python packages - installed via pip
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.100.0
google-ai-generativelanguage>=0.4.0
google-generativeai>=0.3.0
SpeechRecognition>=3.10.0
pyttsx3>=2.90
python-dotenv>=1.0.0
requests>=2.31.0
mediapipe>=0.10.0

