# Guest Assets

To add a custom image or GIF to the Smart Mirror's guest screen:
1. Save your image or GIF file to this folder (`assets/`).
2. Name the file either `guest.gif`, `guest.png`, or `guest.jpg`.
3. The Smart Mirror will automatically detect, load, and render it in the center of the guest advertisement column!
